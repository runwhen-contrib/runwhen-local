apiVersion: runwhen.com/v1
kind: MapCustomizationRules
spec:
  groupRules:
    - match:
        type: pattern
        matchType: base-name
        pattern: "az-lb-health"
        matchMode: substring
        properties: [name]
      group: "{{resource_group.name}}"