    hierarchy: 
    - platform
    {% if match_resource.subscription_name is defined %}
    - subscription_name
    {% else %}
    - subscription_id
    {% endif %}
    - resource_group
    - resource_name

