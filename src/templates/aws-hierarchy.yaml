    hierarchy: 
    - platform
    - account_name
    - region
    {% if qualifiers and qualifiers.resource is defined %}
    - resource_name
    {% elif match_resource.resource_type is defined %}
    - resource_type
    {% endif %}

