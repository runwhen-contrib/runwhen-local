    hierarchy: 
    - platform
    - account_name
    - region
    {% if qualifiers and qualifiers.resource is defined %}
    - resource_name
    {% elif match_resource.name is defined %}
    - resource_name
    {% endif %}

