import type { PackageRule, PackageRuleInputConfig } from '../../config/types';
import { Matcher } from './base';
export declare class DatasourcesMatcher extends Matcher {
    matches({ datasource }: PackageRuleInputConfig, { matchDatasources }: PackageRule): boolean | null;
}
