import type { PackageRule, PackageRuleInputConfig } from '../../config/types';
import { Matcher } from './base';
export declare class JsonataMatcher extends Matcher {
    matches(inputConfig: PackageRuleInputConfig, { matchJsonata }: PackageRule): Promise<boolean | null>;
}
