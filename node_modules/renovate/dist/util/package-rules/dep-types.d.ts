import type { PackageRule, PackageRuleInputConfig } from '../../config/types';
import { Matcher } from './base';
export declare class DepTypesMatcher extends Matcher {
    matches({ depTypes, depType }: PackageRuleInputConfig, { matchDepTypes }: PackageRule): boolean | null;
}
