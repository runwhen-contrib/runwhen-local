import type { PackageRule, PackageRuleInputConfig } from '../../config/types';
import { Matcher } from './base';
export declare class CategoriesMatcher extends Matcher {
    matches({ categories }: PackageRuleInputConfig, { matchCategories }: PackageRule): boolean | null;
}
