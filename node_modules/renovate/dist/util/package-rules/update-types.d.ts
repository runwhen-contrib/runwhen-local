import type { PackageRule, PackageRuleInputConfig } from '../../config/types';
import { Matcher } from './base';
export declare class UpdateTypesMatcher extends Matcher {
    matches({ updateType, isBump }: PackageRuleInputConfig, { matchUpdateTypes }: PackageRule): boolean | null;
}
