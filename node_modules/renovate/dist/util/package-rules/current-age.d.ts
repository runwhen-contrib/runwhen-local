import type { PackageRule, PackageRuleInputConfig } from '../../config/types';
import { Matcher } from './base';
export declare class CurrentAgeMatcher extends Matcher {
    matches({ currentVersionTimestamp }: PackageRuleInputConfig, { matchCurrentAge }: PackageRule): boolean | null;
}
