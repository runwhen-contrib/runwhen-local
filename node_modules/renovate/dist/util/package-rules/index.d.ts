import type { PackageRuleInputConfig } from '../../config/types';
import type { StageName } from '../../types/skip-reason';
export declare function applyPackageRules<T extends PackageRuleInputConfig>(inputConfig: T, stageName?: StageName): Promise<T>;
