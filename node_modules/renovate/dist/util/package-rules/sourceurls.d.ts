import type { PackageRule, PackageRuleInputConfig } from '../../config/types';
import { Matcher } from './base';
export declare class SourceUrlsMatcher extends Matcher {
    matches({ sourceUrl }: PackageRuleInputConfig, { matchSourceUrls }: PackageRule): boolean | null;
}
