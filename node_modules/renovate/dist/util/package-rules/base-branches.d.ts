import type { PackageRule, PackageRuleInputConfig } from '../../config/types';
import { Matcher } from './base';
export declare class BaseBranchesMatcher extends Matcher {
    matches({ baseBranch }: PackageRuleInputConfig, { matchBaseBranches }: PackageRule): boolean | null;
}
