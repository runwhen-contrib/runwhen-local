export declare class Throttle {
    private throttle;
    constructor(interval: number);
    add<T>(task: () => Promise<T>): Promise<T>;
}
export declare function getThrottle(url: string): Throttle | null;
export declare function clear(): void;
