import type { MigratedConfig, RenovateConfig } from './types';
export declare function fixShortHours(input: string): string;
export declare function migrateConfig(config: RenovateConfig, parentKey?: string): MigratedConfig;
