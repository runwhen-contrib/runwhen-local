import type { PackageFile } from '../../../types';
import type { NpmManagerData } from '../../types';
export declare function getLockedVersions(packageFiles: PackageFile<NpmManagerData>[]): Promise<void>;
