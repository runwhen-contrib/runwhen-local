import type { PackageFile } from '../../../types';
import type { NpmManagerData } from '../../types';
export declare function detectMonorepos(packageFiles: Partial<PackageFile<NpmManagerData>>[]): Promise<void>;
