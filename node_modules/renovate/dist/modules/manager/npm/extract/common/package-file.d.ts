import type { PackageFileContent } from '../../../types';
import type { NpmManagerData } from '../../types';
import type { NpmPackage } from '../types';
export declare function extractPackageJson(packageJson: NpmPackage, packageFile: string): PackageFileContent<NpmManagerData> | null;
