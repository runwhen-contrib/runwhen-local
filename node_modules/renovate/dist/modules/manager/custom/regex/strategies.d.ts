import type { PackageDependency } from '../../types';
import type { RegexManagerConfig } from './types';
export declare function handleAny(content: string, packageFile: string, config: RegexManagerConfig): PackageDependency[];
export declare function handleCombination(content: string, packageFile: string, config: RegexManagerConfig): PackageDependency[];
export declare function handleRecursive(content: string, packageFile: string, config: RegexManagerConfig): PackageDependency[];
