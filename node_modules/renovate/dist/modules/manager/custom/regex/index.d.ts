import type { Category } from '../../../../constants';
import type { ExtractConfig, MaybePromise, PackageFileContent } from '../../types';
export declare const categories: Category[];
export declare const defaultConfig: {
    pinDigests: boolean;
};
export declare const supportedDatasources: string[];
export declare const displayName = "Regex";
export declare function extractPackageFile(content: string, packageFile: string, config: ExtractConfig): MaybePromise<PackageFileContent | null>;
