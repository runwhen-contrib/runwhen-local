import type { PackageDependency } from '../types';
export declare const validMatchFields: readonly ["depName", "packageName", "currentValue", "currentDigest", "datasource", "versioning", "extractVersion", "registryUrl", "depType", "indentation"];
export type ValidMatchFields = (typeof validMatchFields)[number];
export declare function isValidDependency({ depName, currentValue, currentDigest, packageName, datasource, }: PackageDependency): boolean;
export declare function checkIsValidDependency(dep: PackageDependency, packageFile: string, manager: string): boolean;
