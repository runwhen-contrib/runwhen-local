import type { ExtractConfig, PackageDependency, PackageFile } from '../types';
import type { ContentDescriptorSpec, GradleManagerData } from './types';
export declare function matchesContentDescriptor(dep: PackageDependency<GradleManagerData>, contentDescriptors?: ContentDescriptorSpec[]): boolean;
export declare function extractAllPackageFiles(config: ExtractConfig, packageFiles: string[]): Promise<PackageFile[] | null>;
