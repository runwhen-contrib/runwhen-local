import type { Fragment, FragmentPath, FragmentUpdater } from './types';
export declare function findCodeFragment(input: string, path: FragmentPath): Fragment | null;
export declare function patchCodeAtFragment(input: string, fragment: Fragment, updater: FragmentUpdater): string;
export declare function patchCodeAtFragments(input: string, fragments: Fragment[], updater: FragmentUpdater): string;
export declare function updateCode(input: string, path: FragmentPath, updater: FragmentUpdater): string;
