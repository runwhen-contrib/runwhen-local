import type { ExtractConfig, PackageDependency, PackageFileContent } from '../types';
import { type Module } from './schema';
export declare function extractPackageFile(content: string, packageFile: string, _config?: ExtractConfig): PackageFileContent | null;
export declare function processModule(module: Module, depType: string): PackageDependency[];
