import { Datasource } from '../datasource';
import type { GetReleasesConfig, ReleaseResult } from '../types';
export declare const supportedApis: Set<string>;
export declare class KubernetesApiDatasource extends Datasource {
    static readonly id = "kubernetes-api";
    constructor();
    defaultVersioning: string;
    getReleases({ packageName, }: GetReleasesConfig): Promise<ReleaseResult | null>;
}
