import { Datasource } from '../datasource';
import type { GetReleasesConfig, ReleaseResult } from '../types';
export declare class GlasskubePackagesDatasource extends Datasource {
    static readonly id = "glasskube-packages";
    static readonly defaultRegistryUrl = "https://packages.dl.glasskube.dev/packages";
    readonly customRegistrySupport = true;
    defaultVersioning: string;
    defaultRegistryUrls: string[];
    constructor();
    getReleases({ packageName, registryUrl, }: GetReleasesConfig): Promise<ReleaseResult | null>;
}
