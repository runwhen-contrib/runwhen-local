import type { Http } from '../../../util/http';
import type { ReleaseResult } from '../types';
export declare function getReleases(http: Http, channel: string, packageName: string): Promise<ReleaseResult | null>;
