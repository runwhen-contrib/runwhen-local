import { Datasource } from '../datasource';
import type { GetReleasesConfig, ReleaseResult } from '../types';
export declare class AwsEKSAddonDataSource extends Datasource {
    static readonly id = "aws-eks-addon";
    readonly defaultVersioning = "aws-eks-addon";
    readonly caching = true;
    private readonly clients;
    constructor();
    getReleases({ packageName: serializedFilter, }: GetReleasesConfig): Promise<ReleaseResult | null>;
    private getClient;
}
