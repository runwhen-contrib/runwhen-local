type OmitFn = (...args: any[]) => any;
export declare function once(callback: () => void, omitFn?: OmitFn): void;
/**
 * Before processing each repository,
 * all keys are supposed to be reset.
 */
export declare function reset(): void;
export {};
