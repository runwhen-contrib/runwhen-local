import type { RenovateConfig } from '../../../config/types';
export type ConfigMigrationResult = {
    result: 'no-migration';
} | {
    result: 'add-checkbox';
} | {
    result: 'pr-exists' | 'pr-modified';
    prNumber: number;
};
export declare function configMigration(config: RenovateConfig, branchList: string[]): Promise<ConfigMigrationResult>;
