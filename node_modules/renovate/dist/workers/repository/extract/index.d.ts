import type { RenovateConfig } from '../../../config/types';
import type { ExtractResult } from '../../types';
export declare function extractAllDependencies(config: RenovateConfig): Promise<ExtractResult>;
