import type { Pr } from '../../../../modules/platform';
import type { BranchConfig } from '../../../types';
export type PrAutomergeBlockReason = 'BranchModified' | 'BranchNotGreen' | 'Conflicted' | 'DryRun' | 'PlatformNotReady' | 'PlatformRejection' | 'off schedule';
export interface AutomergePrResult {
    automerged: boolean;
    branchRemoved?: boolean;
    prAutomergeBlockReason?: PrAutomergeBlockReason;
}
export declare function checkAutoMerge(pr: Pr, config: BranchConfig): Promise<AutomergePrResult>;
