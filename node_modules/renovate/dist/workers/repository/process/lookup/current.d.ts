import type { VersioningApi } from '../../../../modules/versioning/types';
export declare function getCurrentVersion(currentValue: string, lockedVersion: string, versioningApi: VersioningApi, rangeStrategy: string, latestVersion: string, allVersions: string[]): string | null;
