import { Result } from '../../../../util/result';
import type { LookupUpdateConfig, UpdateResult } from './types';
export declare function lookupUpdates(inconfig: LookupUpdateConfig): Promise<Result<UpdateResult, Error>>;
