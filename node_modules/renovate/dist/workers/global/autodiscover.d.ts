import type { AllConfig } from '../../config/types';
export declare function autodiscoverRepositories(config: AllConfig): Promise<AllConfig>;
export declare function applyFilters(repos: string[], filters: string[]): string[];
