import { AwsSdkSigV4AuthInputConfig, AwsSdkSigV4AuthResolvedConfig, AwsSdkSigV4PreviouslyResolved } from "@aws-sdk/core";
import { HandlerExecutionContext, HttpAuthScheme, HttpAuthSchemeParameters, HttpAuthSchemeParametersProvider, HttpAuthSchemeProvider } from "@smithy/types";
import { EKSClientResolvedConfig } from "../EKSClient";
/**
 * @internal
 */
export interface EKSHttpAuthSchemeParameters extends HttpAuthSchemeParameters {
    region?: string;
}
/**
 * @internal
 */
export interface EKSHttpAuthSchemeParametersProvider extends HttpAuthSchemeParametersProvider<EKSClientResolvedConfig, HandlerExecutionContext, EKSHttpAuthSchemeParameters, object> {
}
/**
 * @internal
 */
export declare const defaultEKSHttpAuthSchemeParametersProvider: (config: EKSClientResolvedConfig, context: HandlerExecutionContext, input: object) => Promise<EKSHttpAuthSchemeParameters>;
/**
 * @internal
 */
export interface EKSHttpAuthSchemeProvider extends HttpAuthSchemeProvider<EKSHttpAuthSchemeParameters> {
}
/**
 * @internal
 */
export declare const defaultEKSHttpAuthSchemeProvider: EKSHttpAuthSchemeProvider;
/**
 * @internal
 */
export interface HttpAuthSchemeInputConfig extends AwsSdkSigV4AuthInputConfig {
    /**
     * Configuration of HttpAuthSchemes for a client which provides default identity providers and signers per auth scheme.
     * @internal
     */
    httpAuthSchemes?: HttpAuthScheme[];
    /**
     * Configuration of an HttpAuthSchemeProvider for a client which resolves which HttpAuthScheme to use.
     * @internal
     */
    httpAuthSchemeProvider?: EKSHttpAuthSchemeProvider;
}
/**
 * @internal
 */
export interface HttpAuthSchemeResolvedConfig extends AwsSdkSigV4AuthResolvedConfig {
    /**
     * Configuration of HttpAuthSchemes for a client which provides default identity providers and signers per auth scheme.
     * @internal
     */
    readonly httpAuthSchemes: HttpAuthScheme[];
    /**
     * Configuration of an HttpAuthSchemeProvider for a client which resolves which HttpAuthScheme to use.
     * @internal
     */
    readonly httpAuthSchemeProvider: EKSHttpAuthSchemeProvider;
}
/**
 * @internal
 */
export declare const resolveHttpAuthSchemeConfig: <T>(config: T & HttpAuthSchemeInputConfig & AwsSdkSigV4PreviouslyResolved) => T & HttpAuthSchemeResolvedConfig;
