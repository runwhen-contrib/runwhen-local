export function configNumbers(states, { match }) {
    return {
        ...states,
        $: {
            ...states.$,
            number: {
                t: 'regex',
                type: 'number',
                match,
                chunk: null,
            },
        },
    };
}
//# sourceMappingURL=number.js.map