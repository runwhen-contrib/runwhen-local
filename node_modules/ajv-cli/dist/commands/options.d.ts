import { CodeOptions, Options } from "ajv/dist/2019";
import type { SchemaObject } from "ajv/dist/types";
import type { ParsedArgs } from "minimist";
export declare function checkOptions(schema: SchemaObject, argv: ParsedArgs): string | null;
export declare function getOptions(argv: ParsedArgs): Options & {
    code: CodeOptions;
};
